function ships_struct(faction_array, ship_number) constructor {
	
}

function spawn_level(faction_array, ship_number, ship_number_dev, ship_level) {
	for (var i = 0; i < ship_number; ++i) {
	    // Spawn ships randomly
	}
}